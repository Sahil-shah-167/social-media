import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angrio.template.socialApp',
  appName: 'Socialx',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
